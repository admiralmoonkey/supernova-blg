    <% @comments.each do |comment| %>
        <div class="row">
            <hr>
            <p><%= comment.commenter%> <small><em><%= "#{time_ago_in_words(comment.created_at)} ago" %></em></small></p>
            <p><%= comment.body %></p>
        </div>
    <% end %>
